{
  "name": "api_portfolio_tenx",
  "version": "0.0.0",
  "description": "API Portfolio using TypeScript, ESNext, Node.JS, Express.JS",
  "main": "app.ts",
  "scripts": {
    "dev": "node --watch -r ts-node/register ./src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex (lx-builds-apps)",
  "license": "ISC",
  "dependencies": {
    "express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
